# Dockerfile for MQTT Ingest Service
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ingest/ ./ingest/

CMD ["python", "-m", "ingest.mqtt_ingest"]
